# 品牌设计系统升级记录 — 2026-05-19

基于已有的多智能体品牌设计系统（见 WHAT_I_DID.md），本次在 commit `1aa7a3d6d`（对应 design-output `20260518-2304`）的基础版本上做了一系列增强。

---

## 一、时间戳输出目录（RUN_DIR）

**改动文件**：`.opencode/skills/brand-design/SKILL.md`、`.opencode/agent/planner.md`、`.opencode/agent/designer.md`、`.opencode/agent/critic.md`

每次启动品牌设计 session，Harness 自动生成时间戳目录：

```
RUN_DIR = design-output/YYYYMMDD-HHMM
```

所有输出文件（brief、design-assets、critique、README 等）统一写入 `RUN_DIR/`，多次运行互不覆盖。各子智能体的系统提示也改为"使用 task instructions 中指定的路径"，不再硬编码 `design-output/`。

---

## 二、Stage 1.5 Brief Review（Critic Mode A）

**改动文件**：`.opencode/skills/brand-design/SKILL.md`、`.opencode/agent/critic.md`

在 Planner 产出 brief、Designer 开始生图之前，强制插入一轮 brief 质量评审。

**SKILL.md** 新增 Stage 1.5：派发 `@critic`（Mode A），评审完后展示评分，用户可选择直接进入生图或先让 Planner 修改 brief。

**critic.md** 新增 Mode A / Mode B 双模式：

| 模式 | 触发时机 | 评审对象 | 输出文件 |
|------|---------|---------|---------|
| Mode A | Stage 1.5，生图前 | brief 文本质量 | `brief-review.md` |
| Mode B | Stage 3，生图后 | 生成的视觉资产 | `critique.md` |

Mode A 评分维度：Design Keywords 是否具体、Color Strategy 是否有方向、Symbol & Mark 是否有实质概念、Typography 是否指定了风格类别。

---

## 三、生图安全规则

**改动文件**：`.opencode/agent/designer.md`、`.opencode/agent/critic.md`

### Designer 新增 Hard Rules（生成时预防）

1. **一张图一个主体** — 严禁拼贴 / flat-lay 多物品组合
2. **禁止真实个人信息** — 所有文字使用 placeholder（"Name"、"+00 000 0000"、"hello@brand.com"）
3. **最小化图中文字量** — 只保留必要文字，必须出现的文字在 prompt 中完整写出
4. **Mockup 只允许单一物品** — 一件 T 恤、一张名片、一个手机屏，不得同帧出现多个物品

### Critic Mode B 新增三项强制安全检查（评审时检测）

在 5 维评分之前先跑三项检查，任何 FAIL 都要求重新生成，不受美学分数影响：

| 检查项 | 内容 |
|--------|------|
| 文本幻觉 | 品牌名/标语是否乱码、变形、出现未指定字符 |
| Mockup 单物品规则 | brand-mockup 是否只有一个物品 |
| 隐私信息 | 图中是否含真实姓名、电话、邮箱、地址 |

---

## 四、迭代子目录（iter-N）

**改动文件**：`.opencode/skills/brand-design/SKILL.md`

Stage 4 迭代时，不再覆盖原始文件，改为在 `RUN_DIR/` 下创建递增子目录：

```
design-output/20260519-1430/          ← 原始生成
design-output/20260519-1430/iter-1/   ← 第一次迭代
design-output/20260519-1430/iter-2/   ← 第二次迭代
```

每次迭代后自动对新目录重跑 Critic（Mode B），`ITER_DIR` 成为后续操作的活跃目录。

---

## 五、创意文案生成（Stage 5 + Copywriter Agent）

**新增文件**：`.opencode/agent/copywriter.md`
**改动文件**：`.opencode/skills/brand-design/SKILL.md`

视觉资产全部确认后（含所有迭代），新增可选的 Stage 5，派发 `@copywriter` 生成完整文案包，输出到 `RUN_DIR/copy.md`。

**copywriter.md** 产出五项内容：

| 产出 | 说明 |
|------|------|
| Brand Tagline | 5 个候选（使命/受众/差异化/情感/简短），附推荐理由 |
| Brand Voice Guidelines | 4 组性格形容词对 + 按场景语气说明 + 5 条 Do/Don't |
| Key Messages | 3 条核心信息，各含受众和渠道 |
| Touchpoint Copy | 社媒 bio、About 页开头、活动海报标题/副标题、邮件签名等 |
| Campaign Concept | 有具体活动时触发，产出活动名、创意概念、3 条 headline、CTA |

写作原则来自 [marketingskills](https://github.com/coreyhaines31/marketingskills) 的 copywriting skill：清晰优先、具体数字、展示结果、主动语态、禁用空洞溢美词。

---

## 六、新增独立 Skills

### `social-content` skill

**新增文件**：`.opencode/skills/social-content/SKILL.md`

品牌形象完成后独立调用，基于 brand brief 和 copy.md 生成各平台社媒内容。

- 用户选平台（微信 / 小红书 / 微博 / LinkedIn / Instagram）+ 场合 + 数量
- 先定义 3 个内容支柱（品牌行动 / 社区故事 / 品牌观点）
- 各平台专属规则：微信偏长文暖感、小红书强调首行钩子和标签、微博 ≤140 字加话题标签、LinkedIn 专业但人性化
- 5 种 Hook 公式：好奇 / 故事弧 / 价值前置 / 反常识 / 问句
- 输出到 `RUN_DIR/social-content.md`

### `brand-launch` skill

**新增文件**：`.opencode/skills/brand-launch/SKILL.md`

品牌发布 / 换新策略规划，基于 launch skill 的 ORB 框架。

- Owned / Rented / Borrowed 三类渠道分析，投入优先级：Owned → Rented → Borrowed
- 五阶段计划：内部对齐（-2周）→ 预热（-1周）→ 发布日 → 放大（发布后 1-2 周）→ 常态化（第 2 个月起）
- 每阶段含行动项、渠道、关键信息、即用文案
- 输出到 `RUN_DIR/launch-plan.md`，含成功指标表和风险预案

---

## 当前 Harness 全流程

```
用户输入品牌设计需求
    ↓
Session Setup   生成 RUN_DIR = design-output/YYYYMMDD-HHMM
    ↓
Stage 1         @planner 调研 → RUN_DIR/brief.md
    ↓
Stage 1.5       @critic Mode A 审 brief → RUN_DIR/brief-review.md
                用户选择：继续 / 先修改 brief
    ↓
Stage 2         @designer 生成 5 项视觉资产 → RUN_DIR/
    ↓
Stage 3         @critic Mode B 安全检查 + 5 维评分 → RUN_DIR/critique.md
    ↓
Stage 4（可选） @designer 迭代 → RUN_DIR/iter-N/
                重跑 Stage 3 评新资产
    ↓
Stage 5（可选） @copywriter 生成文案包 → RUN_DIR/copy.md
    ↓
Final           写 RUN_DIR/README.md 汇总报告

独立可调用：
  /social-content  → RUN_DIR/social-content.md
  /brand-launch    → RUN_DIR/launch-plan.md
```
